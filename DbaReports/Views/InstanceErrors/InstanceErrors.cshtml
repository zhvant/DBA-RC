@model IEnumerable<DbaRC.Models.InstanceError>

@{
    ViewData["Title"] = "InstanceErrors";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link rel="stylesheet" type="text/css" media="all" href="css/mystyle.css">

<div class="container body-content">

    <h4 align="center">Instance Log</h4>
    <br />
    <table class="table table-bordered table-condensed table-hover  col-md-6">
        <thead><tr><th>Server</th><th>Date</th><th>Process</th><th>Description</th></tr></thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ServerName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LogDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ProcessInfo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Text)
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <script src="static/scripts/jquery-1.10.2.js"></script>
